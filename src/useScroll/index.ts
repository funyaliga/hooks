import { useState, useEffect, useRef, MutableRefObject } from 'react';


export const useScroll = (
  target: HTMLElement | MutableRefObject<HTMLElement> | Document | Window
) => {
  useEffect(() => {
    
  })
}